
        function pruebita(div) {
        const INSERTAR = document.getElementById(div);
        INSERTAR.innerHTML = '';
        INSERTAR.style.padding="0px";
        }
        function addClasses(respo){
        const INSERTAR = document.getElementById(respo);
        INSERTAR.classList.add("respo");
        }

